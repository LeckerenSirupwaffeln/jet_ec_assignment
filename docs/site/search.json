{"config":{"separator":"[\\s\\-_,:!=\\[\\]()\\\\\"`/]+|\\.(?!\\d)"},"items":[{"location":"apps/example_app.html","level":1,"title":"Example App Reference","text":"<p>This documentation is automatically generated from the source code of <code>example_app</code></p> <p>options:     members: true</p>","path":["Apps","Example App Reference"],"tags":[]},{"location":"apps/example_app.html#example_app","level":2,"title":"<code>example_app</code>","text":"","path":["Apps","Example App Reference"],"tags":[]},{"location":"libs/jet_api.html","level":1,"title":"Jet API Reference","text":"<p>This documentation is automatically generated from the source code of <code>jet_api</code></p> <p>options:     members: true</p>","path":["Libs","Jet API Reference"],"tags":[]},{"location":"libs/jet_api.html#jet_api","level":2,"title":"<code>jet_api</code>","text":"","path":["Libs","Jet API Reference"],"tags":[]},{"location":"libs/jet_api.html#jet_api.AsyncStreamReader","level":3,"title":"<code>AsyncStreamReader</code>","text":"<p>Wraps an async iterable into an async file-like object with a read() method.</p> <p>This utility is primarily used to provide a compatibility layer for libraries (like ijson) that require a \"read()\" interface when consuming data from asynchronous sources like \"httpx\" response streams.</p> Source code in <code>libs/jet_api/src/jet_api/pydantic_streamer.py</code> <pre><code>class AsyncStreamReader:\n    \"\"\"\n    Wraps an async iterable into an async file-like object with a read() method.\n\n    This utility is primarily used to provide a compatibility layer for libraries\n    (like ijson) that require a \"read()\" interface when consuming data from\n    asynchronous sources like \"httpx\" response streams.\n    \"\"\"\n\n    def __init__(self, async_iterable: AsyncIterable[bytes]):\n        \"\"\"\n        Initializes the reader with an asynchronous source of bytes.\n\n        Args:\n            async_iterable: An asynchronous iterable (e.g., a response byte stream)\n                yielding byte chunks.\n        \"\"\"\n\n        self._iterator = aiter(async_iterable)\n        self._buffer = bytearray()\n\n    async def read(self, n: int = -1) -&gt; bytes:\n        \"\"\"\n        Reads up to n bytes from the stream asynchronously.\n\n        If the internal buffer is empty, it awaits the next chunk from the\n        source iterable.\n\n        Args:\n            n: The number of bytes to read. If -1 (default), reads until\n                the stream is exhausted.\n\n        Returns:\n            The bytes read from the stream. Returns an empty byte string\n            to signal End of File (EOF).\n        \"\"\"\n\n        while not self._buffer:\n            try:\n                chunk = await anext(self._iterator)\n                self._buffer.extend(chunk)\n            except StopAsyncIteration:\n                return b\"\"  # Signal true EO\n\n        if n == -1:\n            result = bytes(self._buffer)\n            self._buffer.clear()\n            return result\n\n        result = bytes(self._buffer[:n])\n        del self._buffer[:n]\n        return result\n</code></pre>","path":["Libs","Jet API Reference"],"tags":[]},{"location":"libs/jet_api.html#jet_api.AsyncStreamReader.__init__","level":4,"title":"<code>__init__(async_iterable)</code>","text":"<p>Initializes the reader with an asynchronous source of bytes.</p> <p>Parameters:</p> Name Type Description Default <code>async_iterable</code> <code>AsyncIterable[bytes]</code> <p>An asynchronous iterable (e.g., a response byte stream) yielding byte chunks.</p> required Source code in <code>libs/jet_api/src/jet_api/pydantic_streamer.py</code> <pre><code>def __init__(self, async_iterable: AsyncIterable[bytes]):\n    \"\"\"\n    Initializes the reader with an asynchronous source of bytes.\n\n    Args:\n        async_iterable: An asynchronous iterable (e.g., a response byte stream)\n            yielding byte chunks.\n    \"\"\"\n\n    self._iterator = aiter(async_iterable)\n    self._buffer = bytearray()\n</code></pre>","path":["Libs","Jet API Reference"],"tags":[]},{"location":"libs/jet_api.html#jet_api.AsyncStreamReader.read","level":4,"title":"<code>read(n=-1)</code>  <code>async</code>","text":"<p>Reads up to n bytes from the stream asynchronously.</p> <p>If the internal buffer is empty, it awaits the next chunk from the source iterable.</p> <p>Parameters:</p> Name Type Description Default <code>n</code> <code>int</code> <p>The number of bytes to read. If -1 (default), reads until the stream is exhausted.</p> <code>-1</code> <p>Returns:</p> Type Description <code>bytes</code> <p>The bytes read from the stream. Returns an empty byte string</p> <code>bytes</code> <p>to signal End of File (EOF).</p> Source code in <code>libs/jet_api/src/jet_api/pydantic_streamer.py</code> <pre><code>async def read(self, n: int = -1) -&gt; bytes:\n    \"\"\"\n    Reads up to n bytes from the stream asynchronously.\n\n    If the internal buffer is empty, it awaits the next chunk from the\n    source iterable.\n\n    Args:\n        n: The number of bytes to read. If -1 (default), reads until\n            the stream is exhausted.\n\n    Returns:\n        The bytes read from the stream. Returns an empty byte string\n        to signal End of File (EOF).\n    \"\"\"\n\n    while not self._buffer:\n        try:\n            chunk = await anext(self._iterator)\n            self._buffer.extend(chunk)\n        except StopAsyncIteration:\n            return b\"\"  # Signal true EO\n\n    if n == -1:\n        result = bytes(self._buffer)\n        self._buffer.clear()\n        return result\n\n    result = bytes(self._buffer[:n])\n    del self._buffer[:n]\n    return result\n</code></pre>","path":["Libs","Jet API Reference"],"tags":[]},{"location":"libs/jet_api.html#jet_api.Client","level":3,"title":"<code>Client</code>","text":"<p>An API client dynamically configured via an OpenAPI specification.</p> <p>This client provides a high-level interface to stream data from the API automatically resolving endpoints and base URLs from a local OpenAPI specification file.</p> Source code in <code>libs/jet_api/src/jet_api/client.py</code> <pre><code>class Client:\n    \"\"\"\n    An API client dynamically configured via an OpenAPI specification.\n\n    This client provides a high-level interface to stream data from the API\n    automatically resolving endpoints and base URLs from a local OpenAPI\n    specification file.\n    \"\"\"\n\n    def __init__(self) -&gt; None:\n        \"\"\"\n        Initializes the Client by loading the OpenAPI spec and mapping operations.\n\n        The initialization process retrieves the spec location from application\n        settings, validates that the spec contains at least one server, and\n        pre-maps the 'getRestaurantsByPostcode' operation for faster lookups.\n\n        Raises:\n            ValueError: If the OpenAPI spec is missing servers, contains no\n                valid operations, or is missing the required\n                \"getRestaurantsByPostcode\" operationId.\n        \"\"\"\n\n        logger.info(\"Initializing client...\")\n        spec_traversable = get_settings().OPENAPI_SPEC_TRAVERSABLE\n\n        logger.debug(\"Loading OpenAPI spec...\")\n        self._openapi = None\n        with importlib.resources.as_file(spec_traversable) as file_path:\n            self._openapi = OpenAPI.from_file_path(str(file_path))\n\n        logger.debug(\"Loading base url...\")\n        servers: list[dict[str, Any]] = self._openapi.spec.get(\"servers\", [])\n        if not servers:\n            raise ValueError(\"Found no valid servers in loaded OpenAPI spec\")\n\n        self._base_url = servers[0][\"url\"].rstrip(\"/\")\n\n        logger.debug(\"Loading ops...\")\n        self._ops = {\n            details[\"operationId\"]: (path, method.upper())\n            for path, methods in self._openapi.spec.get(\"paths\", {}).items()\n            for method, details in methods.items()\n            if \"operationId\" in details\n        }\n\n        if not self._ops:\n            raise ValueError(\"Found no valid ops in loaded OpenAPI spec\")\n\n        self._op_get_restaurants_by_postcode = self._ops.get(\n            \"getRestaurantsByPostcode\", None\n        )\n        if not self._op_get_restaurants_by_postcode:\n            raise ValueError(\n                'Found no valid op where \"operationId\" is \"getRestaurantsByPostcode\"'\n            )\n\n        logger.info(\"Client initialized\")\n\n    @asynccontextmanager\n    async def stream_restaurants(\n        self, postcode: str\n    ) -&gt; AsyncIterator[PydanticStreamer[RestaurantsDataResponse]]:\n        \"\"\"\n        Streams restaurant data from the API for a specific postcode.\n\n        This method opens an asynchronous HTTP stream to the resolved endpoint\n        and yields a PydanticStreamer. The streamer handles the incremental\n        parsing of the JSON response, specifically looking for the \"restaurants\" key.\n\n        Args:\n            postcode: A valid UK postcode string (e.g., \"NW1 8NZ\").\n\n        Yields:\n            An instance of PydanticStreamer configured to parse RestaurantsDataResponse objects.\n\n        Raises:\n            ValueError: If the client was not initialized correctly with\n                the required operation.\n            httpx.HTTPStatusError: If the API returns a non-success status code.\n\n        Example:\n            ```python\n            client = Client()\n            async with client.stream_restaurants(\"NW1 8NZ\") as streamer:\n                async for restaurant in streamer:\n                    # \"restaurant\" is yielded as a validated Pydantic model\n                    print(f\"Found: {restaurant.name}\")\n            ```\n        \"\"\"\n\n        if self._openapi is None:\n            raise ValueError(\"OpenAPI spec not initialized\")\n\n        if self._op_get_restaurants_by_postcode is None:\n            raise ValueError(\"Operation not initialized\")\n\n        path_template, method = self._op_get_restaurants_by_postcode\n        full_url = f\"{self._base_url}{path_template.format(postcode=postcode)}\"\n\n        logger.debug(\"Validating request according to OpenAPI specs\")\n        request = httpx.Request(method, full_url)\n        validate_request(HttpxOpenAPIAdapter(request), spec=self._openapi.spec)\n\n        logger.debug(f\"Returning streaming request to {full_url}\")\n\n        async with (\n            httpx.AsyncClient() as client,\n            client.stream(method, full_url) as response,\n        ):\n            response.raise_for_status()\n            streamer = PydanticStreamer(\n                response=response,\n                schema=RestaurantsDataResponse,\n                stream_keys=[\"restaurants\"],\n            )\n            yield streamer\n</code></pre>","path":["Libs","Jet API Reference"],"tags":[]},{"location":"libs/jet_api.html#jet_api.Client.__init__","level":4,"title":"<code>__init__()</code>","text":"<p>Initializes the Client by loading the OpenAPI spec and mapping operations.</p> <p>The initialization process retrieves the spec location from application settings, validates that the spec contains at least one server, and pre-maps the 'getRestaurantsByPostcode' operation for faster lookups.</p> <p>Raises:</p> Type Description <code>ValueError</code> <p>If the OpenAPI spec is missing servers, contains no valid operations, or is missing the required \"getRestaurantsByPostcode\" operationId.</p> Source code in <code>libs/jet_api/src/jet_api/client.py</code> <pre><code>def __init__(self) -&gt; None:\n    \"\"\"\n    Initializes the Client by loading the OpenAPI spec and mapping operations.\n\n    The initialization process retrieves the spec location from application\n    settings, validates that the spec contains at least one server, and\n    pre-maps the 'getRestaurantsByPostcode' operation for faster lookups.\n\n    Raises:\n        ValueError: If the OpenAPI spec is missing servers, contains no\n            valid operations, or is missing the required\n            \"getRestaurantsByPostcode\" operationId.\n    \"\"\"\n\n    logger.info(\"Initializing client...\")\n    spec_traversable = get_settings().OPENAPI_SPEC_TRAVERSABLE\n\n    logger.debug(\"Loading OpenAPI spec...\")\n    self._openapi = None\n    with importlib.resources.as_file(spec_traversable) as file_path:\n        self._openapi = OpenAPI.from_file_path(str(file_path))\n\n    logger.debug(\"Loading base url...\")\n    servers: list[dict[str, Any]] = self._openapi.spec.get(\"servers\", [])\n    if not servers:\n        raise ValueError(\"Found no valid servers in loaded OpenAPI spec\")\n\n    self._base_url = servers[0][\"url\"].rstrip(\"/\")\n\n    logger.debug(\"Loading ops...\")\n    self._ops = {\n        details[\"operationId\"]: (path, method.upper())\n        for path, methods in self._openapi.spec.get(\"paths\", {}).items()\n        for method, details in methods.items()\n        if \"operationId\" in details\n    }\n\n    if not self._ops:\n        raise ValueError(\"Found no valid ops in loaded OpenAPI spec\")\n\n    self._op_get_restaurants_by_postcode = self._ops.get(\n        \"getRestaurantsByPostcode\", None\n    )\n    if not self._op_get_restaurants_by_postcode:\n        raise ValueError(\n            'Found no valid op where \"operationId\" is \"getRestaurantsByPostcode\"'\n        )\n\n    logger.info(\"Client initialized\")\n</code></pre>","path":["Libs","Jet API Reference"],"tags":[]},{"location":"libs/jet_api.html#jet_api.Client.stream_restaurants","level":4,"title":"<code>stream_restaurants(postcode)</code>  <code>async</code>","text":"<p>Streams restaurant data from the API for a specific postcode.</p> <p>This method opens an asynchronous HTTP stream to the resolved endpoint and yields a PydanticStreamer. The streamer handles the incremental parsing of the JSON response, specifically looking for the \"restaurants\" key.</p> <p>Parameters:</p> Name Type Description Default <code>postcode</code> <code>str</code> <p>A valid UK postcode string (e.g., \"NW1 8NZ\").</p> required <p>Yields:</p> Type Description <code>AsyncIterator[PydanticStreamer[RestaurantsDataResponse]]</code> <p>An instance of PydanticStreamer configured to parse RestaurantsDataResponse objects.</p> <p>Raises:</p> Type Description <code>ValueError</code> <p>If the client was not initialized correctly with the required operation.</p> <code>HTTPStatusError</code> <p>If the API returns a non-success status code.</p> Example <pre><code>client = Client()\nasync with client.stream_restaurants(\"NW1 8NZ\") as streamer:\n    async for restaurant in streamer:\n        # \"restaurant\" is yielded as a validated Pydantic model\n        print(f\"Found: {restaurant.name}\")\n</code></pre> Source code in <code>libs/jet_api/src/jet_api/client.py</code> <pre><code>@asynccontextmanager\nasync def stream_restaurants(\n    self, postcode: str\n) -&gt; AsyncIterator[PydanticStreamer[RestaurantsDataResponse]]:\n    \"\"\"\n    Streams restaurant data from the API for a specific postcode.\n\n    This method opens an asynchronous HTTP stream to the resolved endpoint\n    and yields a PydanticStreamer. The streamer handles the incremental\n    parsing of the JSON response, specifically looking for the \"restaurants\" key.\n\n    Args:\n        postcode: A valid UK postcode string (e.g., \"NW1 8NZ\").\n\n    Yields:\n        An instance of PydanticStreamer configured to parse RestaurantsDataResponse objects.\n\n    Raises:\n        ValueError: If the client was not initialized correctly with\n            the required operation.\n        httpx.HTTPStatusError: If the API returns a non-success status code.\n\n    Example:\n        ```python\n        client = Client()\n        async with client.stream_restaurants(\"NW1 8NZ\") as streamer:\n            async for restaurant in streamer:\n                # \"restaurant\" is yielded as a validated Pydantic model\n                print(f\"Found: {restaurant.name}\")\n        ```\n    \"\"\"\n\n    if self._openapi is None:\n        raise ValueError(\"OpenAPI spec not initialized\")\n\n    if self._op_get_restaurants_by_postcode is None:\n        raise ValueError(\"Operation not initialized\")\n\n    path_template, method = self._op_get_restaurants_by_postcode\n    full_url = f\"{self._base_url}{path_template.format(postcode=postcode)}\"\n\n    logger.debug(\"Validating request according to OpenAPI specs\")\n    request = httpx.Request(method, full_url)\n    validate_request(HttpxOpenAPIAdapter(request), spec=self._openapi.spec)\n\n    logger.debug(f\"Returning streaming request to {full_url}\")\n\n    async with (\n        httpx.AsyncClient() as client,\n        client.stream(method, full_url) as response,\n    ):\n        response.raise_for_status()\n        streamer = PydanticStreamer(\n            response=response,\n            schema=RestaurantsDataResponse,\n            stream_keys=[\"restaurants\"],\n        )\n        yield streamer\n</code></pre>","path":["Libs","Jet API Reference"],"tags":[]},{"location":"libs/jet_api.html#jet_api.PydanticStreamer","level":3,"title":"<code>PydanticStreamer</code>","text":"<p>A streaming parser that yields validated Pydantic models from a JSON stream.</p> <p>This class leverages \"ijson\" to perform \"incremental\" parsing, allowing the application to process individual items in a large JSON array as they arrive over the network, rather than waiting for the entire response body.</p> <p>          Class Type Parameters:        </p> Name Bound or Constraints Description Default <code>T</code> <code>BaseModel</code> <p>The Pydantic BaseModel class representing the full response schema.</p> required Source code in <code>libs/jet_api/src/jet_api/pydantic_streamer.py</code> <pre><code>class PydanticStreamer[T: BaseModel]:\n    \"\"\"\n    A streaming parser that yields validated Pydantic models from a JSON stream.\n\n    This class leverages \"ijson\" to perform \"incremental\" parsing, allowing\n    the application to process individual items in a large JSON array as they\n    arrive over the network, rather than waiting for the entire response body.\n\n    Type Args:\n        T: The Pydantic BaseModel class representing the full response schema.\n    \"\"\"\n\n    def __init__(\n        self, response: httpx.Response, schema: type[T], stream_keys: Sequence[str]\n    ) -&gt; None:\n        \"\"\"\n        Initializes the streamer and inspects the schema for target model types.\n\n        The initializer performs introspection on the provided Pydantic schema\n        to determine the inner types of the fields specified in \"stream_keys\".\n        This ensures that individual items can be validated correctly during\n        the streaming process.\n\n        Args:\n            response: The active httpx response object (usually from a stream).\n            schema: The Pydantic model class for the total response.\n            stream_keys: A list of field names in the schema that contain\n                lists of items to be yielded individually.\n\n        Raises:\n            ValueError: If a key in \"stream_keys\" is not present in the schema.\n            TypeError: If a specified field lacks the necessary type annotations\n                to determine the item type.\n        \"\"\"\n\n        self.response = response\n        self.schema = schema\n\n        self._final_data: dict[str, Any] = {}\n        self.stream_models: dict[str, type[BaseModel]] = {}\n\n        for key in stream_keys:\n            if key not in schema.model_fields:\n                raise ValueError(\n                    f'Invalid stream_key \"{key}\". Valid keys: {list(schema.model_fields)}'\n                )\n\n            annotation = schema.model_fields[key].annotation\n            if annotation is None:\n                raise TypeError(f\"Field {key} lacks a type annotation\")\n\n            if get_origin(annotation) in (Union, types.UnionType):\n                for arg in get_args(annotation):\n                    if get_origin(arg) is list:\n                        annotation = arg\n                        break\n\n            extracted_type = get_args(annotation)[0]\n            self.stream_models[key] = cast(type[BaseModel], extracted_type)\n\n    async def __aiter__(self) -&gt; AsyncIterator[BaseModel]:\n        stream_reader = AsyncStreamReader(self.response.aiter_bytes())\n\n        parser = ijson.parse_async(stream_reader)\n        builder = ijson.ObjectBuilder()\n\n        async for prefix, event, value in parser:\n            builder.event(event, value)\n\n            if event == \"end_map\" and prefix.endswith(\".item\"):\n                field_name = prefix[:-5]  # remove \".item\" to get the field name\n\n                if field_name in self.stream_models:\n                    model_class = self.stream_models[field_name]\n                    raw_item = builder.value[field_name][-1]\n\n                    yield model_class.model_validate(raw_item)\n\n        self._final_data = builder.value\n\n    def __await__(self) -&gt; Generator[Any, None, T]:\n        return self._consume().__await__()\n\n    async def _consume(self) -&gt; T:\n        async for _ in self:\n            pass  # Exhaust the stream fully\n\n        return self.schema.model_validate(self._final_data)\n</code></pre>","path":["Libs","Jet API Reference"],"tags":[]},{"location":"libs/jet_api.html#jet_api.PydanticStreamer.__init__","level":4,"title":"<code>__init__(response, schema, stream_keys)</code>","text":"<p>Initializes the streamer and inspects the schema for target model types.</p> <p>The initializer performs introspection on the provided Pydantic schema to determine the inner types of the fields specified in \"stream_keys\". This ensures that individual items can be validated correctly during the streaming process.</p> <p>Parameters:</p> Name Type Description Default <code>response</code> <code>Response</code> <p>The active httpx response object (usually from a stream).</p> required <code>schema</code> <code>type[T]</code> <p>The Pydantic model class for the total response.</p> required <code>stream_keys</code> <code>Sequence[str]</code> <p>A list of field names in the schema that contain lists of items to be yielded individually.</p> required <p>Raises:</p> Type Description <code>ValueError</code> <p>If a key in \"stream_keys\" is not present in the schema.</p> <code>TypeError</code> <p>If a specified field lacks the necessary type annotations to determine the item type.</p> Source code in <code>libs/jet_api/src/jet_api/pydantic_streamer.py</code> <pre><code>def __init__(\n    self, response: httpx.Response, schema: type[T], stream_keys: Sequence[str]\n) -&gt; None:\n    \"\"\"\n    Initializes the streamer and inspects the schema for target model types.\n\n    The initializer performs introspection on the provided Pydantic schema\n    to determine the inner types of the fields specified in \"stream_keys\".\n    This ensures that individual items can be validated correctly during\n    the streaming process.\n\n    Args:\n        response: The active httpx response object (usually from a stream).\n        schema: The Pydantic model class for the total response.\n        stream_keys: A list of field names in the schema that contain\n            lists of items to be yielded individually.\n\n    Raises:\n        ValueError: If a key in \"stream_keys\" is not present in the schema.\n        TypeError: If a specified field lacks the necessary type annotations\n            to determine the item type.\n    \"\"\"\n\n    self.response = response\n    self.schema = schema\n\n    self._final_data: dict[str, Any] = {}\n    self.stream_models: dict[str, type[BaseModel]] = {}\n\n    for key in stream_keys:\n        if key not in schema.model_fields:\n            raise ValueError(\n                f'Invalid stream_key \"{key}\". Valid keys: {list(schema.model_fields)}'\n            )\n\n        annotation = schema.model_fields[key].annotation\n        if annotation is None:\n            raise TypeError(f\"Field {key} lacks a type annotation\")\n\n        if get_origin(annotation) in (Union, types.UnionType):\n            for arg in get_args(annotation):\n                if get_origin(arg) is list:\n                    annotation = arg\n                    break\n\n        extracted_type = get_args(annotation)[0]\n        self.stream_models[key] = cast(type[BaseModel], extracted_type)\n</code></pre>","path":["Libs","Jet API Reference"],"tags":[]}]}